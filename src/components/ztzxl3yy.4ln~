.projet {
    // left: 50%;
    flex: 0 0 auto;
    width: 100vw;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: nowrap;
    flex-direction: row;
    position: relative;
    background: #101439;
    transform: translate(0,0);
    transition: .25s all;


    &.is--active {
        transform: translate(-50%,0);
        transition: .25s all;
    }

    &__picture {
        height: 100%;
        width: 50%;

        &_img {
            height: 100%;
            width: 100%;
            object-fit: cover;
        }
    }

    &__description {
        width: 50%;
        height: 100%;

        &_wrapper{
            max-width: 320px;
        }

        &_title {
        }

        &_tags {
        }

        &_txt {
        }
    }
}
